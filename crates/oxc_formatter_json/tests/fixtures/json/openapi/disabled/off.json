// With the option off, an OpenAPI document is untouched. Same shape as `../tables.json`, which is
// reordered, so the pair pins that the option is what does it.
{
  "info": { "version": "1.0.0", "title": "Demo" },
  "openapi": "3.0.0",
  "paths": { "/pets": { "get": { "responses": {}, "summary": "List pets", "operationId": "listPets" } } }
}
